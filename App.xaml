<Application x:Class="Lunex.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Lunex"
             xmlns:vm="clr-namespace:Lunex.ViewModels"
             xmlns:views="clr-namespace:Lunex.Views"
             xmlns:components="clr-namespace:Lunex.Components"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!-- Value Converters -->
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <local:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
        <local:NullOrEmptyToCollapsedConverter x:Key="NullOrEmptyToCollapsedConverter"/>
        <local:GameToIconConverter x:Key="GameToIconConverter"/>

        <!-- ========================================== -->
        <!-- COLOR & DESIGN SYSTEM TOKENS               -->
        <!-- ========================================== -->
        <!-- Color Constants -->
        <Color x:Key="BackgroundColor">#101010</Color>
        <Color x:Key="SurfaceColor">#1E1E1E</Color>
        <Color x:Key="SurfaceContainerColor">#161616</Color>
        <Color x:Key="SurfaceContainerHighColor">#242424</Color>
        <Color x:Key="SurfaceContainerLowColor">#1A1A1A</Color>
        <Color x:Key="SurfaceContainerLowestColor">#0A0A0A</Color>

        <Color x:Key="PrimaryColor">#D97757</Color>
        <Color x:Key="PrimaryContainerColor">#D97757</Color>
        <Color x:Key="OnPrimaryContainerColor">#FFFFFF</Color>
        <Color x:Key="SecondaryColor">#3A3A3A</Color>
        <Color x:Key="SecondaryContainerColor">#2E2E2E</Color>
        <Color x:Key="TertiaryColor">#4A4A4A</Color>
        <Color x:Key="OnSurfaceColor">#E5E7EB</Color>
        <Color x:Key="OnSurfaceVariantColor">#9CA3AF</Color>
        <Color x:Key="OutlineColor">#2A2A2A</Color>
        <Color x:Key="ErrorColor">#EF4444</Color>

        <!-- Standard SolidColorBrush Resources -->
        <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}" />
        <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource SurfaceColor}" />
        <SolidColorBrush x:Key="SurfaceContainerBrush" Color="{StaticResource SurfaceContainerColor}" />
        <SolidColorBrush x:Key="SurfaceContainerHighBrush" Color="{StaticResource SurfaceContainerHighColor}" />
        <SolidColorBrush x:Key="SurfaceContainerLowBrush" Color="{StaticResource SurfaceContainerLowColor}" />
        <SolidColorBrush x:Key="SurfaceContainerLowestBrush" Color="{StaticResource SurfaceContainerLowestColor}" />
        
        <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}" />
        <SolidColorBrush x:Key="PrimaryContainerBrush" Color="{StaticResource PrimaryContainerColor}" />
        <SolidColorBrush x:Key="OnPrimaryContainerBrush" Color="{StaticResource OnPrimaryContainerColor}" />
        <SolidColorBrush x:Key="SecondaryBrush" Color="{StaticResource SecondaryColor}" />
        <SolidColorBrush x:Key="SecondaryContainerBrush" Color="{StaticResource SecondaryContainerColor}" />
        <SolidColorBrush x:Key="TertiaryBrush" Color="{StaticResource TertiaryColor}" />
        <SolidColorBrush x:Key="OnSurfaceBrush" Color="{StaticResource OnSurfaceColor}" />
        <SolidColorBrush x:Key="OnSurfaceVariantBrush" Color="{StaticResource OnSurfaceVariantColor}" />
        <SolidColorBrush x:Key="OutlineBrush" Color="{StaticResource OutlineColor}" />
        <SolidColorBrush x:Key="ErrorBrush" Color="{StaticResource ErrorColor}" />

        <!-- Opacity-tinted brushes -->
        <SolidColorBrush x:Key="PrimaryBrushLow" Color="{StaticResource PrimaryColor}" Opacity="0.08" />
        <SolidColorBrush x:Key="PrimaryBrushMedium" Color="{StaticResource PrimaryColor}" Opacity="0.15" />
        <SolidColorBrush x:Key="PrimaryBrushHigh" Color="{StaticResource PrimaryColor}" Opacity="0.75" />
        <SolidColorBrush x:Key="PrimaryBrushVeryHigh" Color="{StaticResource PrimaryColor}" Opacity="0.85" />

        <SolidColorBrush x:Key="SecondaryBrushLow" Color="{StaticResource SecondaryColor}" Opacity="0.08" />
        <SolidColorBrush x:Key="SecondaryBrushMedium" Color="{StaticResource SecondaryColor}" Opacity="0.15" />
        <SolidColorBrush x:Key="SecondaryBrushHigh" Color="{StaticResource SecondaryColor}" Opacity="0.25" />
        <SolidColorBrush x:Key="SecondaryBrushVeryHigh" Color="{StaticResource SecondaryColor}" Opacity="0.40" />

        <SolidColorBrush x:Key="TertiaryBrushLow" Color="{StaticResource TertiaryColor}" Opacity="0.08" />
        <SolidColorBrush x:Key="TertiaryBrushMedium" Color="{StaticResource TertiaryColor}" Opacity="0.15" />
        <SolidColorBrush x:Key="TertiaryBrushHigh" Color="{StaticResource TertiaryColor}" Opacity="0.25" />
        <SolidColorBrush x:Key="TertiaryBrushVeryHigh" Color="{StaticResource TertiaryColor}" Opacity="0.40" />

        <SolidColorBrush x:Key="OnSurfaceVariantBrushLow" Color="{StaticResource OnSurfaceVariantColor}" Opacity="0.15" />
        <SolidColorBrush x:Key="OnSurfaceVariantBrushMedium" Color="{StaticResource OnSurfaceVariantColor}" Opacity="0.50" />
        <SolidColorBrush x:Key="OnSurfaceVariantBrushHigh" Color="{StaticResource OnSurfaceVariantColor}" Opacity="0.70" />
        <SolidColorBrush x:Key="OnSurfaceVariantBrushVeryHigh" Color="{StaticResource OnSurfaceVariantColor}" Opacity="0.85" />

        <!-- Ambient container glows (disabled/subtle dark background) -->
        <SolidColorBrush x:Key="PrimaryContainerBrushAmbient" Color="#000000" Opacity="0.1" />
        <SolidColorBrush x:Key="SecondaryContainerBrushAmbient" Color="#000000" Opacity="0.05" />

        <!-- Flat Brushes instead of gradients -->
        <SolidColorBrush x:Key="CyberBackgroundGradient" Color="{StaticResource BackgroundColor}" />
        <SolidColorBrush x:Key="PrimaryAccentGradient" Color="{StaticResource PrimaryColor}" />

        <!-- Typography Settings -->
        <FontFamily x:Key="MonospaceFont">Consolas, JetBrains Mono, Courier New</FontFamily>
        <FontFamily x:Key="DefaultFont">pack://application:,,,/Fonts/#Rajdhani</FontFamily>

        <!-- atmosphere repeating diagonal lines overlay -->
        <VisualBrush x:Key="DiagonalLinesPatternBrush" TileMode="Tile" Viewport="0,0,16,16" ViewportUnits="Absolute">
            <VisualBrush.Visual>
                <Grid Width="16" Height="16" Background="Transparent">
                    <Line X1="0" Y1="16" X2="16" Y2="0" Stroke="White" StrokeThickness="1.5" Opacity="0.04" />
                </Grid>
            </VisualBrush.Visual>
        </VisualBrush>

        <!-- ========================================== -->
        <!-- SCROLLBAR CUSTOM STYLING                   -->
        <!-- ========================================== -->
        <Style x:Key="ScrollBarThumbStyle" TargetType="Thumb">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="IsTabStop" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border x:Name="rectangleBorder" 
                                CornerRadius="4" 
                                Background="White" 
                                Opacity="0.12" 
                                SnapsToDevicePixels="True" />
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="rectangleBorder" Property="Opacity" Value="0.3" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ScrollBar">
            <Setter Property="Width" Value="8" />
            <Setter Property="Height" Value="Auto" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid x:Name="Bg" SnapsToDevicePixels="true">
                            <Track x:Name="PART_Track" IsDirectionReversed="true">
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource ScrollBarThumbStyle}" />
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Width" Value="Auto" />
                    <Setter Property="Height" Value="8" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ScrollBar">
                                <Grid x:Name="Bg" SnapsToDevicePixels="true">
                                    <Track x:Name="PART_Track" Orientation="Horizontal" IsDirectionReversed="False">
                                        <Track.Thumb>
                                            <Thumb Style="{StaticResource ScrollBarThumbStyle}" />
                                        </Track.Thumb>
                                    </Track>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- ========================================== -->
        <!-- TEXTBOX ROUNDED GLASS STYLE                -->
        <!-- ========================================== -->
        <Style x:Key="SunkenSearchBox" TargetType="TextBox">
            <Setter Property="Background" Value="#121212" />
            <Setter Property="BorderBrush" Value="#2A2A2A" />
            <Setter Property="BorderThickness" Value="0,0,0,1" />
            <Setter Property="Foreground" Value="{StaticResource OnSurfaceBrush}" />
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="CaretBrush" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <Border x:Name="border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}">
                                <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock x:Name="watermark"
                                       Text="{TemplateBinding Tag}"
                                       Foreground="{StaticResource OnSurfaceVariantBrush}"
                                       Opacity="0.4"
                                       FontStyle="Italic"
                                       Visibility="Collapsed"
                                       IsHitTestVisible="False"
                                       Margin="12,0,0,0"
                                       VerticalAlignment="Center"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="watermark" Property="Visibility" Value="Visible" />
                            </Trigger>
                            <Trigger Property="Text" Value="{x:Null}">
                                <Setter TargetName="watermark" Property="Visibility" Value="Visible" />
                            </Trigger>
                            <Trigger Property="IsFocused" Value="true">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- ========================================== -->
        <!-- PREMIUM SURFACE PANEL STYLE (FORMERLY GLASS) -->
        <!-- ========================================== -->
        <Style x:Key="GlassPanelStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource OutlineBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" BlurRadius="8" ShadowDepth="1" Opacity="0.15"/>
                </Setter.Value>
            </Setter>
        </Style>


        <!-- ========================================== -->
        <!-- PREMIUM COMBOBOX FLAT SYSTEM STYLE        -->
        <!-- ========================================== -->
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#121212"/>
            <Setter Property="BorderBrush" Value="#2A2A2A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="12,6,12,6"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.CanContentScroll" Value="true"/>
            <Setter Property="ScrollViewer.PanningMode" Value="Both"/>
            <Setter Property="Stylus.IsFlicksEnabled" Value="False"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid x:Name="templateRoot" SnapsToDevicePixels="true">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition MinWidth="{DynamicResource {x:Static SystemParameters.VerticalScrollBarWidthKey}}" Width="0"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- Main background border -->
                            <Border x:Name="backgroundBorder" 
                                    Grid.ColumnSpan="2" 
                                    Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                    BorderThickness="{TemplateBinding BorderThickness}" 
                                    CornerRadius="6" />

                            <ToggleButton x:Name="toggleButton" 
                                          Grid.ColumnSpan="2" 
                                          Background="Transparent"
                                          BorderThickness="0" 
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" 
                                          Cursor="Hand">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="Transparent" BorderThickness="0">
                                            <!-- Arrow path -->
                                            <Path x:Name="arrow" 
                                                  Data="M7 10l5 5 5-5H7z" 
                                                  Fill="#B3C7C4D8" 
                                                  HorizontalAlignment="Right" 
                                                  VerticalAlignment="Center" 
                                                  Margin="0,0,12,0" 
                                                  Width="12" Height="12" 
                                                  Stretch="Uniform"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="true">
                                                <Setter TargetName="arrow" Property="Fill" Value="White"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            
                            <ContentPresenter x:Name="contentPresenter" 
                                              Content="{TemplateBinding SelectionBoxItem}" 
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" 
                                              ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}" 
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                              Margin="{TemplateBinding Padding}" 
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" 
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}" 
                                              IsHitTestVisible="false"/>

                            <!-- Popup dropdown list -->
                            <Popup x:Name="PART_Popup" 
                                   AllowsTransparency="true" 
                                   Grid.ColumnSpan="2" 
                                   IsOpen="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" 
                                   Margin="1" 
                                   Placement="Bottom" 
                                   PopupAnimation="{DynamicResource {x:Static SystemParameters.ComboBoxPopupAnimationKey}}">
                                <Border x:Name="dropDownBorder" 
                                        Background="#121212" 
                                        BorderBrush="#2A2A2A" 
                                        BorderThickness="1" 
                                        CornerRadius="6" 
                                        MaxHeight="{TemplateBinding MaxDropDownHeight}" 
                                        MinWidth="{Binding ActualWidth, ElementName=backgroundBorder}"
                                        Margin="0,4,0,4">
                                    <Border.Effect>
                                        <DropShadowEffect Color="#000000" BlurRadius="12" Opacity="0.3" ShadowDepth="2"/>
                                    </Border.Effect>
                                    <ScrollViewer x:Name="DropDownScrollViewer">
                                        <Grid x:Name="grid" RenderOptions.ClearTypeHint="Enabled">
                                            <Canvas x:Name="canvas" HorizontalAlignment="Left" Height="0" VerticalAlignment="Top" Width="0">
                                                <Rectangle x:Name="opaqueRect" Fill="{Binding Background, ElementName=dropDownBorder}" Height="{Binding ActualHeight, ElementName=dropDownBorder}" Width="{Binding ActualWidth, ElementName=dropDownBorder}"/>
                                            </Canvas>
                                            <ItemsPresenter x:Name="ItemsPresenter" KeyboardNavigation.DirectionalNavigation="Contained" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                        </Grid>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="backgroundBorder" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                <Setter TargetName="backgroundBorder" Property="Background" Value="#161616"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="true">
                                <Setter TargetName="backgroundBorder" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                <Setter TargetName="backgroundBorder" Property="Background" Value="#161616"/>
                            </Trigger>
                            <Trigger Property="IsDropDownOpen" Value="true">
                                <Setter TargetName="backgroundBorder" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                <Setter TargetName="backgroundBorder" Property="Background" Value="#161616"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Premium ComboBoxItem Style -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="ItemBorder" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="4" 
                                Padding="{TemplateBinding Padding}" 
                                Margin="4,2" 
                                SnapsToDevicePixels="True">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#2A2A2A"/>
                                <Setter TargetName="ItemBorder" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="true">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{StaticResource PrimaryBrush}"/>
                                <Setter TargetName="ItemBorder" Property="BorderBrush" Value="Transparent"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Global Segoe UI Typography Styles -->
        <!-- Global implicit styles using the DefaultFont -->
        <Style TargetType="Control">
            <Setter Property="FontFamily" Value="{StaticResource DefaultFont}" />
            <Setter Property="FontSize" Value="13.5" />
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="{StaticResource DefaultFont}" />
            <Setter Property="FontSize" Value="13.5" />
        </Style>

        <!-- Premium Round Thumb Slider Style -->
        <Style TargetType="Slider">
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Border Background="Transparent" SnapsToDevicePixels="true">
                            <Grid x:Name="GridColumn" Height="20">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto" MinHeight="20"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                
                                <!-- Track Background Line -->
                                <Border x:Name="TrackBackground" 
                                        Grid.Row="1" 
                                        Height="4" 
                                        CornerRadius="2" 
                                        Background="{TemplateBinding Background}" 
                                        VerticalAlignment="Center"/>
                                
                                <!-- Track Area -->
                                <Track x:Name="PART_Track" Grid.Row="1">
                                    <Track.DecreaseRepeatButton>
                                        <RepeatButton Command="{x:Static Slider.DecreaseLarge}" Background="{TemplateBinding Foreground}" Height="4" BorderThickness="0">
                                            <RepeatButton.Template>
                                                <ControlTemplate TargetType="RepeatButton">
                                                    <Border Background="{TemplateBinding Background}" CornerRadius="2"/>
                                                </ControlTemplate>
                                            </RepeatButton.Template>
                                        </RepeatButton>
                                    </Track.DecreaseRepeatButton>
                                    <Track.IncreaseRepeatButton>
                                        <RepeatButton Command="{x:Static Slider.IncreaseLarge}" Background="Transparent" Height="4" BorderThickness="0">
                                            <RepeatButton.Template>
                                                <ControlTemplate TargetType="RepeatButton">
                                                    <Border Background="{TemplateBinding Background}"/>
                                                </ControlTemplate>
                                            </RepeatButton.Template>
                                        </RepeatButton>
                                    </Track.IncreaseRepeatButton>
                                    <Track.Thumb>
                                        <Thumb x:Name="SliderThumb" Width="14" Height="14" Cursor="Hand">
                                            <Thumb.Template>
                                                <ControlTemplate TargetType="Thumb">
                                                    <Grid>
                                                        <Ellipse x:Name="ThumbEllipse" 
                                                                 Fill="White" 
                                                                 Stroke="#33FFFFFF" 
                                                                 StrokeThickness="1">
                                                            <Ellipse.Effect>
                                                                <DropShadowEffect Color="#000000" BlurRadius="4" Opacity="0.3" ShadowDepth="1"/>
                                                            </Ellipse.Effect>
                                                        </Ellipse>
                                                    </Grid>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="ThumbEllipse" Property="Fill" Value="#F4A28C"/>
                                                            <Setter TargetName="ThumbEllipse" Property="Stroke" Value="{StaticResource PrimaryBrush}"/>
                                                        </Trigger>
                                                        <Trigger Property="IsDragging" Value="True">
                                                            <Setter TargetName="ThumbEllipse" Property="Fill" Value="{StaticResource PrimaryBrush}"/>
                                                            <Setter TargetName="ThumbEllipse" Property="Stroke" Value="White"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Thumb.Template>
                                        </Thumb>
                                    </Track.Thumb>
                                </Track>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ========================================== -->
        <!-- DEFAULT NEUTRAL DARK BUTTON STYLE          -->
        <!-- ========================================== -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="#222222"/>
            <Setter Property="BorderBrush" Value="#2A2A2A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="{StaticResource OnSurfaceBrush}"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                CornerRadius="6"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2A2A2A"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#3E3E3E"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1A1A1A"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#2A2A2A"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#55FFFFFF"/>
                                <Setter TargetName="border" Property="Background" Value="#1A1A1A"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#222222"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ========================================== -->
        <!-- PLAY BUTTON PREMIUM GRADIENT STYLE         -->
        <!-- ========================================== -->
        <Style x:Key="PlayButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="24,10"/>
            <Setter Property="FontFamily" Value="{StaticResource DefaultFont}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <ControlTemplate.Resources>
                            <Storyboard x:Key="HoverOn">
                                <DoubleAnimation Storyboard.TargetName="border" Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)" To="1.02" Duration="0:0:0.1"/>
                                <DoubleAnimation Storyboard.TargetName="border" Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)" To="1.02" Duration="0:0:0.1"/>
                                <ColorAnimation Storyboard.TargetName="border" Storyboard.TargetProperty="Background.(LinearGradientBrush.GradientStops)[0].Color" To="#FF6428" Duration="0:0:0.1"/>
                                <ColorAnimation Storyboard.TargetName="border" Storyboard.TargetProperty="Background.(LinearGradientBrush.GradientStops)[1].Color" To="#DE4C16" Duration="0:0:0.1"/>
                            </Storyboard>
                            <Storyboard x:Key="HoverOff">
                                <DoubleAnimation Storyboard.TargetName="border" Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)" To="1.0" Duration="0:0:0.1"/>
                                <DoubleAnimation Storyboard.TargetName="border" Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)" To="1.0" Duration="0:0:0.1"/>
                                <ColorAnimation Storyboard.TargetName="border" Storyboard.TargetProperty="Background.(LinearGradientBrush.GradientStops)[0].Color" To="#E8541A" Duration="0:0:0.1"/>
                                <ColorAnimation Storyboard.TargetName="border" Storyboard.TargetProperty="Background.(LinearGradientBrush.GradientStops)[1].Color" To="#C94010" Duration="0:0:0.1"/>
                            </Storyboard>
                            <Storyboard x:Key="PressedOn">
                                <DoubleAnimation Storyboard.TargetName="border" Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)" To="0.97" Duration="0:0:0.05"/>
                                <DoubleAnimation Storyboard.TargetName="border" Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)" To="0.97" Duration="0:0:0.05"/>
                                <ColorAnimation Storyboard.TargetName="border" Storyboard.TargetProperty="Background.(LinearGradientBrush.GradientStops)[0].Color" To="#B23C0F" Duration="0:0:0.05"/>
                                <ColorAnimation Storyboard.TargetName="border" Storyboard.TargetProperty="Background.(LinearGradientBrush.GradientStops)[1].Color" To="#962A05" Duration="0:0:0.05"/>
                            </Storyboard>
                        </ControlTemplate.Resources>
                        <Border x:Name="border"
                                CornerRadius="4"
                                BorderThickness="0"
                                Padding="{TemplateBinding Padding}"
                                RenderTransformOrigin="0.5,0.5">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                    <GradientStop Color="#E8541A" Offset="0"/>
                                    <GradientStop Color="#C94010" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <Border.RenderTransform>
                                <ScaleTransform ScaleX="1.0" ScaleY="1.0"/>
                            </Border.RenderTransform>
                            <Border.Effect>
                                <DropShadowEffect Color="#C94010" Opacity="0.4" BlurRadius="12" ShadowDepth="0"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <EventTrigger RoutedEvent="MouseEnter">
                                <BeginStoryboard Storyboard="{StaticResource HoverOn}"/>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="MouseLeave">
                                <BeginStoryboard Storyboard="{StaticResource HoverOff}"/>
                            </EventTrigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard Storyboard="{StaticResource PressedOn}"/>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard Storyboard="{StaticResource HoverOn}"/>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- NeonButtonStyle — alias for PlayButtonStyle (used in dialogs and empty-state CTAs) -->
        <Style x:Key="NeonButtonStyle" TargetType="Button" BasedOn="{StaticResource PlayButtonStyle}"/>

        <!-- ========================================== -->
        <!-- ICON BUTTON STYLE                          -->
        <!-- ========================================== -->
        <Style x:Key="IconButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="20">
                            <Border.Background>
                                <SolidColorBrush x:Name="bgBrush" Color="Transparent"/>
                            </Border.Background>
                            <TextElement.Foreground>
                                <SolidColorBrush x:Name="fgBrush" Color="#888888"/>
                            </TextElement.Foreground>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <EventTrigger RoutedEvent="MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="fgBrush" Storyboard.TargetProperty="Color" To="White" Duration="0:0:0.1"/>
                                        <ColorAnimation Storyboard.TargetName="bgBrush" Storyboard.TargetProperty="Color" To="#22FFFFFF" Duration="0:0:0.1"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="fgBrush" Storyboard.TargetProperty="Color" To="#888888" Duration="0:0:0.1"/>
                                        <ColorAnimation Storyboard.TargetName="bgBrush" Storyboard.TargetProperty="Color" To="Transparent" Duration="0:0:0.1"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modal Dialog Close Button Style -->
        <Style x:Key="ModalCloseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Width" Value="24"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="12" Background="Transparent" SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1CFFFFFF"/>
                                <Setter Property="Foreground" Value="#FFA185"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2CFFFFFF"/>
                                <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ========================================== -->

        <!-- SAVE ARGUMENTS BUTTON STYLE                -->
        <!-- ========================================== -->
        <Style x:Key="SaveArgsButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="4" Padding="6">
                            <Border.Background>
                                <SolidColorBrush x:Name="bgBrush" Color="Transparent"/>
                            </Border.Background>
                            <TextElement.Foreground>
                                <SolidColorBrush x:Name="fgBrush" Color="#888888"/>
                            </TextElement.Foreground>
                            <Path x:Name="savePath" 
                                  Data="M17 3H5c-1.11 0-2 .9-2 2v14c0 1.1.89 2 2 2h14c1.1 0 2-.9 2-2V7l-4-4zm-5 16c-1.66 0-3-1.34-3-3s1.34-3 3-3 3 1.34 3 3-1.34 3-3 3zm3-10H5V5h10v4z" 
                                  Fill="{Binding ElementName=border, Path=(TextElement.Foreground)}" 
                                  Width="14" Height="14" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <EventTrigger RoutedEvent="MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="fgBrush" Storyboard.TargetProperty="Color" To="{StaticResource PrimaryColor}" Duration="0:0:0.1"/>
                                        <ColorAnimation Storyboard.TargetName="bgBrush" Storyboard.TargetProperty="Color" To="#22FFFFFF" Duration="0:0:0.1"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="fgBrush" Storyboard.TargetProperty="Color" To="#888888" Duration="0:0:0.1"/>
                                        <ColorAnimation Storyboard.TargetName="bgBrush" Storyboard.TargetProperty="Color" To="Transparent" Duration="0:0:0.1"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ========================================== -->

        <!-- DATA TEMPLATE VIEW SWITCHING MAP           -->
        <!-- ========================================== -->
        <DataTemplate DataType="{x:Type vm:SplashViewModel}">
            <views:SplashView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:LibraryViewModel}">
            <views:LibraryView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:GameDetailsViewModel}">
            <views:GameDetailsView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:MusicViewModel}">
            <views:MusicView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:StatsViewModel}">
            <views:StatsView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ProfileViewModel}">
            <views:ProfileView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:SettingsViewModel}">
            <views:SettingsView />
        </DataTemplate>

        <!-- Style for CachedContentControl to override default template -->
        <Style TargetType="{x:Type components:CachedContentControl}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type components:CachedContentControl}">
                        <Border x:Name="PART_Container"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- Global implicit style to enable smooth scrolling on all ScrollViewers -->
        <Style TargetType="ScrollViewer">
            <Setter Property="components:ScrollViewerHelper.IsSmoothScrollEnabled" Value="True"/>
        </Style>
    </Application.Resources>
</Application>
